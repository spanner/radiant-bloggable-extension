%p#choose_Event
  = f.label :subject_id, "Choose an event", :class => 'inline'
  %span.note
    to display as a calendar block at the bottom of your text
  %br
  = f.select :subject_id, Event.all.map {|e| ["#{e.date}: #{e.title}", e.id]}, {}, :class => 'textbox'
